# Feature Specification: Cloud-Edge System Integration (Phase 9)

**Feature ID**: 002-cloud-edge-integration  
**Created**: 2025-11-04  
**Status**: Planning  
**Priority**: P0 (Core Integration)

---

## 📋 Executive Summary

本功能规格定义 Cloud 端系统与 Edge 端储能柜系统的完整联调方案，实现云边协同的数据同步、指令下发、许可证管理和监控告警等核心功能。

### 目标

1. **数据同步**：Edge→Cloud 批量数据上传（传感器数据、设备状态、告警信息）
2. **指令下发**：Cloud→Edge 实时指令（配置管理、许可证管理、远程控制）
3. **许可证管理**：Cloud 端许可证签发和验证，Edge 端实时更新
4. **状态监控**：实时监控 Edge 端状态，告警聚合和分析

### 成功标准

- ✅ Edge 端数据成功同步到 Cloud 端（成功率 ≥99.9%）
- ✅ Cloud 指令成功下发到 Edge 并执行（响应时间 ≤3秒）
- ✅ 许可证验证和更新正常工作（延迟 ≤500ms）
- ✅ 系统支持至少 100 个储能柜并发连接
- ✅ 通过完整的集成测试和端到端测试

---

## 🎯 User Scenarios

### Scenario 1: Edge 数据批量同步到 Cloud

**角色**: Edge 端系统（自动化）  
**目标**: 每 5 分钟将本地采集的传感器数据批量上传到 Cloud 端  

**前置条件**:
- Edge 端已成功注册到 Cloud 端
- Edge 端有有效的 API Key
- Edge 端已采集到传感器数据

**操作流程**:
1. Edge 端定时器触发（每 5 分钟）
2. Edge 端从本地数据库查询未同步的数据（最多 1000 条）
3. Edge 端调用 Cloud API：`POST /api/v1/cabinets/:cabinet_id/sync`
4. Cloud 端验证 API Key 和许可证
5. Cloud 端批量插入传感器数据到 TimescaleDB
6. Cloud 端返回同步结果（成功条数、失败条数）
7. Edge 端更新本地数据的 `synced` 标志

**成功标准**:
- 数据完整性：所有字段正确保存
- 性能：1000 条数据同步完成时间 ≤2 秒
- 可靠性：网络异常时 Edge 端能重试（最多 3 次）

**失败场景**:
- 许可证已过期：Cloud 返回 403，Edge 停止同步并告警
- 网络超时：Edge 端重试 3 次后记录失败日志
- 数据格式错误：Cloud 返回 400，Edge 端记录错误数据

---

### Scenario 2: Cloud 下发配置更新指令

**角色**: Cloud 端管理员  
**目标**: 远程更新储能柜的告警阈值  

**前置条件**:
- Cloud 端已连接到 MQTT Broker
- Edge 端已订阅指令 Topic
- 管理员已登录 Cloud 端 Web 界面

**操作流程**:
1. 管理员在 Web 界面选择目标储能柜
2. 管理员修改 CO2 告警阈值从 5000ppm 改为 5500ppm
3. Cloud 端创建指令记录（存入数据库）
4. Cloud 端通过 MQTT 发布指令到 Topic: `cloud/cabinets/{cabinet_id}/commands/config`
5. Edge 端接收指令并解析
6. Edge 端验证参数合法性
7. Edge 端更新本地配置文件并重载告警检测器
8. Edge 端通过 MQTT 发布响应到 Topic: `cloud/cabinets/{cabinet_id}/responses/{cmd_id}`
9. Cloud 端接收响应并更新指令状态
10. Web 界面显示指令执行结果

**成功标准**:
- 端到端响应时间 ≤5 秒
- 指令执行成功率 ≥99%
- Web 界面实时显示执行进度

---

### Scenario 3: 许可证更新和验证

**角色**: Cloud 端管理员  
**目标**: 更新储能柜的许可证并立即生效  

**前置条件**:
- Cloud 端已生成新许可证
- Edge 端许可证即将过期（剩余 7 天）

**操作流程**:
1. Cloud 端检测到许可证即将过期，发送告警通知
2. 管理员在 Web 界面创建新许可证（延期 1 年）
3. Cloud 端签发许可证并保存到数据库
4. Cloud 端通过 MQTT 推送许可证更新指令
5. Edge 端接收新许可证并验证签名
6. Edge 端更新内存中的许可证缓存
7. Edge 端保存许可证到本地文件
8. Edge 端响应更新成功
9. Cloud 端更新许可证状态为"已激活"

**成功标准**:
- 许可证更新延迟 ≤3 秒
- Edge 端无需重启即可生效
- 许可证签名验证正确

---

### Scenario 4: 告警聚合和分析

**角色**: Cloud 端监控系统（自动化）  
**目标**: 实时接收 Edge 端告警并进行聚合分析  

**前置条件**:
- Edge 端告警系统正常运行
- Cloud 端 MQTT 订阅了告警 Topic

**操作流程**:
1. Edge 端检测到 CO2 浓度超过阈值
2. Edge 端生成告警记录（本地数据库）
3. Edge 端通过 MQTT 推送告警到 `edge/cabinets/{cabinet_id}/alerts`
4. Cloud 端接收告警并解析
5. Cloud 端保存告警到数据库
6. Cloud 端分析告警严重程度并触发通知
7. Cloud 端 Web 界面实时显示告警
8. Cloud 端聚合分析：检测是否有多个储能柜同时告警（群体异常）

**成功标准**:
- 告警延迟：从 Edge 生成到 Cloud 显示 ≤5 秒
- 告警完整性：所有告警都能正确接收
- 聚合分析：能识别群体异常模式

---

## 📊 Key Entities

### 1. EdgeCabinet (Edge 储能柜)

Cloud 端视角的 Edge 端储能柜实体。

**属性**:
- `cabinet_id` (string, PK): 储能柜唯一标识
- `name` (string): 储能柜名称
- `mac_address` (string): MAC 地址
- `location` (string): 物理位置
- `edge_version` (string): Edge 端系统版本
- `connection_status` (enum): 连接状态（online/offline）
- `last_seen_at` (timestamp): 最后在线时间
- `last_sync_at` (timestamp): 最后同步时间
- `mqtt_status` (enum): MQTT 连接状态（connected/disconnected）
- `api_key` (string): Edge 端的 API Key（加密存储）
- `sync_interval` (int): 数据同步间隔（秒）
- `created_at` (timestamp): 创建时间
- `updated_at` (timestamp): 更新时间

**关系**:
- 一个 EdgeCabinet 有多个 Sensors（传感器设备）
- 一个 EdgeCabinet 有一个 License（许可证）
- 一个 EdgeCabinet 有多个 Commands（指令记录）
- 一个 EdgeCabinet 有多个 EdgeAlerts（Edge 端告警）

---

### 2. EdgeSyncData (Edge 同步数据)

Edge 端批量同步到 Cloud 的数据载荷。

**属性**:
- `cabinet_id` (string): 储能柜 ID
- `sync_time` (timestamp): 同步时间
- `data_count` (int): 数据条数
- `payload` (JSON): 数据载荷
  - `devices` (array): 设备列表
  - `sensor_data` (array): 传感器数据列表
  - `alerts` (array): 告警列表
  - `system_status` (object): Edge 系统状态

---

### 3. CloudCommand (Cloud 指令)

Cloud 端下发到 Edge 端的指令。

**属性**:
- `command_id` (string, PK): 指令唯一标识（UUID）
- `cabinet_id` (string, FK): 目标储能柜 ID
- `command_type` (enum): 指令类型（config_update/license_update/query_status/service_restart等）
- `command_category` (enum): 指令分类（config/license/query/control）
- `payload` (JSON): 指令参数
- `status` (enum): 指令状态（pending/sent/success/failed/timeout）
- `sent_at` (timestamp): 发送时间
- `response_at` (timestamp): 响应时间
- `response_data` (JSON): 响应数据
- `error_message` (string): 错误信息
- `operator` (string): 操作员
- `timeout` (int): 超时时间（秒）
- `retry_count` (int): 重试次数
- `created_at` (timestamp): 创建时间

---

### 4. EdgeAlert (Edge 端告警)

从 Edge 端同步的告警记录。

**属性**:
- `alert_id` (string, PK): 告警 ID（Edge 端生成）
- `cabinet_id` (string, FK): 储能柜 ID
- `device_id` (string): 设备 ID
- `alert_type` (enum): 告警类型（threshold_exceeded/device_offline/system_error）
- `severity` (enum): 严重级别（low/medium/high/critical）
- `sensor_type` (enum): 传感器类型
- `value` (float): 当前值
- `threshold` (float): 阈值
- `message` (string): 告警消息
- `timestamp` (timestamp): 告警时间
- `resolved` (boolean): 是否已解决
- `resolved_at` (timestamp): 解决时间
- `synced_at` (timestamp): 同步到 Cloud 的时间

---

## 🔗 System Interactions

### Edge→Cloud 数据流

```
┌─────────────────────────────────────────────────┐
│              Edge 端系统                         │
│                                                 │
│  ┌────────────────────────────────────────┐   │
│  │  数据采集模块                            │   │
│  │  - 7种传感器数据                         │   │
│  │  - 设备心跳                              │   │
│  │  - 告警检测                              │   │
│  └────────┬───────────────────────────────┘   │
│           │                                    │
│  ┌────────▼───────────────────────────────┐   │
│  │  本地数据库 (SQLite)                     │   │
│  │  - sensor_data (未同步标记)             │   │
│  │  - alerts                               │   │
│  │  - devices                              │   │
│  └────────┬───────────────────────────────┘   │
│           │                                    │
│  ┌────────▼───────────────────────────────┐   │
│  │  Cloud Sync Service                     │   │
│  │  - 定时器: 5分钟                         │   │
│  │  - 批量打包: 最多1000条                  │   │
│  │  - 重试机制: 3次                         │   │
│  └────────┬───────────────────────────────┘   │
└───────────┼─────────────────────────────────┘
            │
            │ HTTP POST /api/v1/cabinets/:cabinet_id/sync
            │ Header: X-API-Key: {edge_api_key}
            │
┌───────────▼─────────────────────────────────┐
│              Cloud 端系统                     │
│                                               │
│  ┌─────────────────────────────────────────┐ │
│  │  Edge Sync Handler                       │ │
│  │  1. 验证 API Key                         │ │
│  │  2. 验证许可证                            │ │
│  │  3. 解析数据载荷                          │ │
│  │  4. 批量插入数据库                        │ │
│  └────────┬────────────────────────────────┘ │
│           │                                   │
│  ┌────────▼────────────────────────────────┐ │
│  │  数据库层                                 │ │
│  │  - TimescaleDB (sensor_data)            │ │
│  │  - PostgreSQL (alerts, devices)         │ │
│  └─────────────────────────────────────────┘ │
└───────────────────────────────────────────────┘
```

---

### Cloud→Edge 指令流

```
┌─────────────────────────────────────────────────┐
│              Cloud 端系统                         │
│                                                 │
│  ┌────────────────────────────────────────┐   │
│  │  Web 管理界面 / API                      │   │
│  │  - 管理员创建指令                         │   │
│  │  - 选择目标储能柜                         │   │
│  │  - 填写指令参数                           │   │
│  └────────┬───────────────────────────────┘   │
│           │                                    │
│  ┌────────▼───────────────────────────────┐   │
│  │  Command Service                        │   │
│  │  1. 保存指令到数据库                     │   │
│  │  2. 生成 command_id (UUID)             │   │
│  │  3. 发布 MQTT 消息                      │   │
│  └────────┬───────────────────────────────┘   │
│           │                                    │
│  ┌────────▼───────────────────────────────┐   │
│  │  MQTT Client (Publisher)                │   │
│  │  Topic: cloud/cabinets/{id}/commands/{cat}│   │
│  │  QoS: 1 (至少一次)                       │   │
│  └────────┬───────────────────────────────┘   │
└───────────┼─────────────────────────────────┘
            │
            │ MQTT Publish
            │
       ┌────▼────┐
       │  MQTT   │
       │  Broker │
       └────┬────┘
            │
            │ MQTT Subscribe
            │
┌───────────▼─────────────────────────────────┐
│              Edge 端系统                      │
│                                               │
│  ┌─────────────────────────────────────────┐ │
│  │  MQTT Client (Subscriber)                │ │
│  │  Subscribe: cloud/cabinets/{id}/commands/#│ │
│  └────────┬────────────────────────────────┘ │
│           │                                   │
│  ┌────────▼────────────────────────────────┐ │
│  │  Command Handler                         │ │
│  │  1. 解析指令                              │ │
│  │  2. 验证权限                              │ │
│  │  3. 执行指令                              │ │
│  │  4. 生成响应                              │ │
│  └────────┬────────────────────────────────┘ │
│           │                                   │
│  ┌────────▼────────────────────────────────┐ │
│  │  MQTT Client (Publisher)                 │ │
│  │  Topic: cloud/cabinets/{id}/responses/{cmd_id}│ │
│  └────────┬────────────────────────────────┘ │
└───────────┼─────────────────────────────────┘
            │
            │ MQTT Publish (Response)
            │
┌───────────▼─────────────────────────────────┐
│              Cloud 端系统                     │
│                                               │
│  ┌─────────────────────────────────────────┐ │
│  │  MQTT Client (Subscriber)                │ │
│  │  Subscribe: cloud/cabinets/+/responses/+ │ │
│  └────────┬────────────────────────────────┘ │
│           │                                   │
│  ┌────────▼────────────────────────────────┐ │
│  │  Response Handler                        │ │
│  │  1. 解析响应                              │ │
│  │  2. 更新指令状态                          │ │
│  │  3. 通知 Web 界面                         │ │
│  └─────────────────────────────────────────┘ │
└───────────────────────────────────────────────┘
```

---

## 📏 Measurable Success Criteria

### 1. 功能完整性

- [ ] Edge→Cloud 数据同步功能完整实现
  - [ ] 传感器数据同步（7 种传感器类型）
  - [ ] 设备状态同步
  - [ ] 告警信息同步
  - [ ] 批量同步支持（最多 1000 条/次）

- [ ] Cloud→Edge 指令下发功能完整实现
  - [ ] P0 指令：配置更新（储能柜 ID、告警阈值）
  - [ ] P0 指令：许可证管理（更新、吊销）
  - [ ] P1 指令：远程查询（状态、配置、设备列表）
  - [ ] P2 指令：远程控制（重启服务、切换模式）

- [ ] 许可证管理功能完整实现
  - [ ] Cloud 端签发许可证
  - [ ] Cloud 端吊销许可证
  - [ ] Edge 端验证许可证（HTTP API）
  - [ ] Edge 端更新许可证（MQTT 推送）

### 2. 性能指标

- [ ] 数据同步性能
  - [ ] 1000 条数据同步时间 ≤ 2 秒
  - [ ] 同步成功率 ≥ 99.9%
  - [ ] 网络异常时能自动重试（最多 3 次）

- [ ] 指令下发性能
  - [ ] 指令端到端响应时间 ≤ 5 秒
  - [ ] 指令执行成功率 ≥ 99%
  - [ ] MQTT 消息送达保证（QoS 1）

- [ ] 许可证验证性能
  - [ ] 许可证验证响应时间 ≤ 500ms
  - [ ] 许可证更新延迟 ≤ 3 秒

### 3. 可靠性和容错

- [ ] 支持至少 100 个储能柜并发连接
- [ ] Cloud 端不可达时，Edge 端能独立运行 24 小时
- [ ] MQTT 断线自动重连（指数退避）
- [ ] 数据同步失败时能记录失败日志并重试

### 4. 安全性

- [ ] Edge→Cloud 数据同步使用 API Key 认证
- [ ] Cloud→Edge 指令下发使用 MQTT over TLS
- [ ] 许可证使用 RSA-2048 签名验证
- [ ] 所有敏感操作记录审计日志

### 5. 测试覆盖

- [ ] 单元测试覆盖率 ≥ 70%
- [ ] 集成测试覆盖所有 API 端点
- [ ] 端到端测试覆盖完整的数据流和指令流
- [ ] 性能测试验证并发能力

---

## 🚫 Out of Scope

以下功能**不在本次 Phase 9 范围内**：

1. **Edge 端代码修改**：本次只规划 Cloud 端的适配工作，Edge 端的 API 和 MQTT Topic 已定义，无需修改
2. **固件升级（OTA）**：固件升级功能优先级为 P3，留待未来实现
3. **Web 管理界面大幅改造**：只添加必要的 Edge 管理页面，不进行界面重构
4. **性能优化**：只满足基本性能要求，不进行深度优化
5. **多租户支持**：本阶段只支持单租户，多租户功能留待未来
6. **数据导出和报表**：数据分析和报表功能不在本次范围

---

## 🔄 Dependencies

### External Dependencies

1. **MQTT Broker**
   - 版本：Mosquitto 2.0+ 或 EMQX 5.0+
   - 配置：支持 TLS、ACL 权限控制
   - 部署：Cloud 端部署或使用云服务

2. **Edge 端系统**
   - 版本：Edge System v2.0+
   - 要求：已实现 Cloud Sync 和 MQTT Subscriber 功能
   - 文档：Edge_ALL_API.md

### Internal Dependencies

1. **许可证签发服务**
   - 状态：已实现（internal/services/license_service.go）
   - 需要：配置 RSA 密钥对

2. **MQTT Client**
   - 状态：已实现（internal/mqtt/client.go）
   - 需要：调整 Topic 命名规范以匹配 Edge 端

3. **Command Service**
   - 状态：已实现（internal/services/command_service.go）
   - 需要：扩展支持更多指令类型

---

## 📚 References

1. **Edge_ALL_API.md**: Edge 端系统完整 API 文档
2. **senddata.md**: Cloud→Edge 指令下发功能清单
3. **Cloud端功能需求.md**: Cloud 端系统需求文档
4. **constitution.md**: 项目原则和技术栈约束
5. **MQTT Best Practices**: https://www.hivemq.com/mqtt-essentials/
6. **TimescaleDB Documentation**: https://docs.timescale.com/

---

## ✅ Acceptance Criteria

本功能规格完成的验收标准：

1. **文档完整性**
   - [x] 完成功能规格说明（spec.md）
   - [ ] 完成实施计划（plan.md）
   - [ ] 完成技术调研（research.md）
   - [ ] 完成数据模型设计（data-model.md）
   - [ ] 完成 API 契约定义（contracts/）
   - [ ] 完成快速开始指南（quickstart.md）

2. **团队评审**
   - [ ] 产品经理确认需求覆盖
   - [ ] 技术负责人确认架构合理
   - [ ] 开发团队确认可实施
   - [ ] 测试团队确认可测试

3. **原则合规性**
   - [ ] 符合项目原则第 I 条：云边协同架构
   - [ ] 符合项目原则第 II 条：数据驱动与实时监控
   - [ ] 符合项目原则第 III 条：安全与权限控制
   - [ ] 符合项目原则第 IV 条：可观测性与容错性
   - [ ] 符合项目原则第 V 条：测试驱动开发

---

**Next Steps**: 进入 Phase 1（研究和设计），完成技术调研和详细设计。

