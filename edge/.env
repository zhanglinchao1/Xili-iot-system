# Edge 系统环境变量 - 测试环境
# 当前配置适用于三个项目在同一台机器的测试环境

# 部署环境 (test 或 production)
DEPLOYMENT_ENV=test

# Cloud 端点 (测试环境使用 localhost)
CLOUD_ENDPOINT=http://localhost:8003/api/v1

# MQTT Bridge 地址 (测试环境 Cloud MQTT 端口映射)
MQTT_BRIDGE_ADDRESS=127.0.0.1:18883

# 生产环境配置示例 (当 Cloud 部署到云服务器时取消注释并修改)
# DEPLOYMENT_ENV=production
# CLOUD_ENDPOINT=https://cloud.example.com/api/v1
# MQTT_BRIDGE_ADDRESS=cloud.example.com:8883
